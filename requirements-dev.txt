# ─────────────────────────────────────────────────────────────────────────────
# requirements-dev.txt
# Root-level dev dependencies for local development (NOT for Docker containers).
# Each service (backend/, model_server/, airflow/) has its own requirements.txt
# for use inside their respective Docker images.
#
# Setup:
#   python -m venv .venv
#   .venv\Scripts\activate        # Windows PowerShell
#   source .venv/bin/activate     # WSL2 / Linux / Mac
#   pip install -r requirements-dev.txt
# ─────────────────────────────────────────────────────────────────────────────

# ── Version Control & Pipeline ────────────────────────────────────────────
dvc==3.49.0

# ── Experiment Tracking ───────────────────────────────────────────────────
mlflow==2.11.1

# ── Testing ───────────────────────────────────────────────────────────────
pytest==8.1.1
pytest-cov==5.0.0
pytest-asyncio==0.23.6
httpx==0.27.0

# ── Code Quality ──────────────────────────────────────────────────────────
black==24.4.2
flake8==7.0.0
isort==5.13.2

# ── FastAPI (local dev) ───────────────────────────────────────────────────
fastapi==0.110.0
uvicorn[standard]==0.29.0
pydantic==2.7.0
pydantic-settings==2.2.1
python-dotenv==1.0.1
python-multipart==0.0.9

# ── ML ────────────────────────────────────────────────────────────────────
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.4.2
xgboost==2.0.3
joblib==1.4.0
scipy==1.13.0

# ── Computer Vision ───────────────────────────────────────────────────────
opencv-python==4.9.0.80
mediapipe==0.10.11

# ── Monitoring & Logging ──────────────────────────────────────────────────
prometheus-client==0.20.0
structlog==24.1.0